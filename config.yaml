project_name: "test-build"
dist_default: "el7"
cache_dir: ".rpm_cache_test" # Local cache for testing
build_root: "rpmbuild_test"  # Local build root

components:
  - name: "redis"
    version: "6.2.0"
    type: "infra"
    spec: "bak/buildrpm/hk-component-redis.spec"
    source_dir: "mock_src/redis" # 相对路径测试
    validate_exists:
      - "dummy_code.c"
    pre_build: "echo 'Running pre-build' > prebuild.log"
    tags: ["infra", "components"]

groups:
  components: ["infra"]
