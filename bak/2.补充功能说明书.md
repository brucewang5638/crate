这是一个非常合理的架构升级需求。目前的脚本将 `components` 视为一个整体（黑盒），导致你无法单独修复或构建其中的某一个基础组件（比如只想修 `redis`，却被迫重跑 `mysql` 和 `nginx`）。

要实现**“原子化构建”**（即既支持构建单个组件，又支持构建原来的大类），我们需要对脚本的**配置数据结构**和**参数解析逻辑**进行重构。

### 核心设计理念

1. **拆分定义 (Registry)**：将原来的大类拆解为具体的“原子组件”（如 `common`, `mariadb`, `redis`）。
2. **引入组的概念 (Groups)**：保留 `components` 这样的标签，作为一个“别名组”，方便批量操作。
3. **脚本参数化**：`build_components.sh` 不再是无脑跑所有，而是要能接收参数（如 `./build_components.sh mariadb`）。

---

### 🛠️ 改造方案

请按照以下步骤修改你的 `build.sh`。

#### 1. 修改配置区：引入“组件注册表”和“分组”

替换原来的 `VERSIONS` 和 `COMPONENTS` 定义部分：

```bash
# ========== 1. 组件注册表 (原子级控制) ==========
# 格式: ["组件名"]="版本号|构建脚本|RPM匹配模式"
# 注意：构建脚本支持传参，例如 "build_infra.sh redis"
declare -A REGISTRY=(
    # --- 核心进程 ---
    ["procmate"]="1.5.3|build_procmate.sh|procmate-*.rpm"

    # --- 基础组件 (原 components 拆分) ---
    ["common"]="1.0.0|build_infra.sh common|hk-component-common-*.rpm"
    ["mariadb"]="10.5.0|build_infra.sh mariadb|hk-component-mariadb-*.rpm"
    ["redis"]="6.2.0|build_infra.sh redis|hk-component-redis-*.rpm"

    # --- 业务服务 (原 services 拆分) ---
    ["smzjg"]="1.0.0|build_services.sh smzjg|hk-service-smzjg-*.rpm"
    ["gateway"]="1.2.0|build_services.sh gateway|hk-service-gateway-*.rpm"
)

# ========== 2. 分组定义 (快捷别名) ==========
# 允许使用 --rebuild components 一次性构建列表中的所有项
declare -A GROUPS=(
    ["all"]="procmate common mariadb redis smzjg gateway"
    ["components"]="common mariadb redis"
    ["services"]="smzjg gateway"
)

```

#### 2. 修改参数解析逻辑：支持组展开

我们需要一个函数，把用户输入的 `components` 自动展开成 `common mariadb redis`。

在 `print_usage` 函数之后，添加这个解析函数，并替换原来的 `while` 循环逻辑：

```bash
# ... (print_usage 函数保持不变) ...

# --- 辅助函数：解析目标列表（支持组名展开）---
resolve_targets() {
    local input_str="$1"
    local resolved=()

    IFS=',' read -ra INPUTS <<< "$input_str"
    for item in "${INPUTS[@]}"; do
        # 1. 检查是否是组名 (存在于 GROUPS 数组)
        if [[ ${GROUPS[$item]+_} ]]; then
            # 展开组，递归添加
            for sub_item in ${GROUPS[$item]}; do
                resolved+=("$sub_item")
            done
        # 2. 检查是否是原子组件 (存在于 REGISTRY 数组)
        elif [[ ${REGISTRY[$item]+_} ]]; then
            resolved+=("$item")
        else
            echo "❌ 错误: 未知的组件或组名: $item"
            exit 1
        fi
    done
    # 输出去重后的列表
    echo "${resolved[@]}" | tr ' ' '\n' | sort -u | tr '\n' ' '
}

# ========== 参数解析 (重写) ==========
USE_CACHE_TARGETS=()
REBUILD_TARGETS=()
# ... (其他变量初始化不变) ...

while [[ $# -gt 0 ]]; do
    case $1 in
        --use-cache)
            # 调用函数解析组
            read -ra USE_CACHE_TARGETS <<< "$(resolve_targets "$2")"
            shift 2
            ;;
        --rebuild)
            read -ra REBUILD_TARGETS <<< "$(resolve_targets "$2")"
            shift 2
            ;;
        # ... (其他 case 保持不变，如 --external, --arch 等) ...
    esac
done

# 如果未指定任何操作，默认 rebuild all
if [ ${#USE_CACHE_TARGETS[@]} -eq 0 ] && [ ${#REBUILD_TARGETS[@]} -eq 0 ] && [ -z "$EXTERNAL_RPM_DIR" ] && [ "$SHOW_VERSIONS" = false ]; then
    echo "ℹ️  未指定参数，默认全量重新构建..."
    read -ra REBUILD_TARGETS <<< "$(resolve_targets "all")"
fi

```

#### 3. 修改主流程：适配新的注册表结构

替换原来的构建循环逻辑（即 `步骤1: 从缓存复用` 和 `步骤2: 重新构建` 部分）：

```bash
    # ========== 步骤1: 从缓存复用 RPM ==========
    for comp in "${USE_CACHE_TARGETS[@]}"; do
        # 解析元数据: 版本|脚本|正则
        IFS='|' read -r version script pattern <<< "${REGISTRY[$comp]}"

        echo ""
        echo "📦 [缓存复用] 组件: $comp (v$version)"

        cached_rpms=$(find "$CACHE_DIR" -name "$pattern" 2>/dev/null)

        if [ -z "$cached_rpms" ]; then
            echo "⚠️  警告: 缓存未命中，降级为重新构建 -> $comp"
            REBUILD_TARGETS+=("$comp")
        else
            cp $cached_rpms "$REPO_DIR/"
            count=$(echo "$cached_rpms" | wc -l)
            echo "   ✅ 已复用 $count 个文件"
        fi
    done

    # ========== 步骤2: 重新构建指定组件 ==========
    # 去重：防止同时在 use-cache 和 rebuild 中出现（以 rebuild 为准）
    # 这里简单处理，直接遍历
    for comp in "${REBUILD_TARGETS[@]}"; do
        IFS='|' read -r version script_cmd pattern <<< "${REGISTRY[$comp]}"

        # 拆分脚本命令和参数 (例如 "build_infra.sh redis")
        set -- $script_cmd
        script_file="$1"
        script_arg="$2"  # 可能为空

        if [ ! -f "$script_file" ]; then
            echo "❌ 错误: 找不到构建脚本 $script_file (组件: $comp)"
            exit 1
        fi

        echo ""
        echo "🔧 [构建中] 组件: $comp"
        echo "   版本: $version"
        echo "   脚本: $script_file ${script_arg:-}"

        # 执行构建：传入 ARCH, DIST, VERSION, 和可选的组件子参数
        # 注意：这里传参顺序约定为: $1=ARCH $2=DIST $3=VERSION $4=SUB_COMPONENT
        bash "./$script_file" "$ARCH" "$DIST" "$version" "$script_arg"

        # ... (后续的缓存清理和复制逻辑，只需将原来写死的 pattern 变量换成上面的 $pattern 即可) ...

        # 清理旧缓存
        find "$CACHE_DIR" -name "$pattern" -delete 2>/dev/null || true

        # 采集新产物
        new_rpms=$(find "$BUILD_ROOT/RPMS" -name "$pattern" 2>/dev/null)
        if [ -z "$new_rpms" ]; then
            echo "❌ 错误: 构建完成后未找到符合 $pattern 的产物！"
            exit 1
        else
            echo "$new_rpms" | while read rpm; do
                cp "$rpm" "$CACHE_DIR/"
                cp "$rpm" "$REPO_DIR/"
            done
            echo "   ✅ 构建成功并更新缓存"
        fi
    done

```

---

### 🔗 关键配套：修改子构建脚本

为了配合上面的改动，你的子脚本（例如 `build_components.sh`）也需要改造，让它支持只构建某一个部分。

**旧的 `build_components.sh` (假设):**

```bash
#!/bin/bash
# ... 编译 redis ...
# ... 编译 mariadb ...
# ... 编译 common ...

```

**新的 `build_infra.sh` (建议重命名):**

```bash
#!/bin/bash
ARCH=$1
DIST=$2
VERSION=$3
TARGET=$4  # 接收第4个参数

echo "启动基础组件构建: 目标=$TARGET"

build_redis() {
    echo "Building Redis..."
    # ...
}

build_mariadb() {
    echo "Building MariaDB..."
    # ...
}

# 路由逻辑
case "$TARGET" in
    redis)
        build_redis
        ;;
    mariadb)
        build_mariadb
        ;;
    common)
        # build_common
        ;;
    *)
        # 如果没有传参数，或者传了 unknown，可能默认跑所有？
        # 建议这里报错，强制要求精确控制
        echo "❌ 请指定要构建的子组件 (redis/mariadb/common)"
        exit 1
        ;;
esac

```

### 💡 这样改的好处

1. **精确控制**：
* `./build.sh --rebuild redis` -> 只重新编译 Redis，速度极快。
* `./build.sh --use-cache components --rebuild smzjg` -> 基础环境全复用，只重编业务代码。


2. **向下兼容**：
* `./build.sh --rebuild components` -> 依然有效，脚本会自动把它展开成 `common mariadb redis` 并逐个调用构建。


3. **清晰的注册表**：
* 所有版本号、文件名匹配规则、对应的脚本都在 `REGISTRY` 数组里一目了然，以后新增组件只需要改这一处。